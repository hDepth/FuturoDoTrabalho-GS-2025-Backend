# Gamification API (Oracle) - Scaffold

This is a scaffolded Node.js + Express API configured to use Oracle Database via node-oracledb.
Follow the README to install Oracle Instant Client and set up the DB. Use the create_tables.sql to create tables.
